#!/usr/bin/env bash

function main {
    cd ~/Documents/second-brain.md

    while true; do
        backup-commit;
        n-backup-commit --stage-all

        if [ $# -eq 1 ]; then
            sleep $1
        else
            sleep 30m;
        fi
    done
}

main "$@"
