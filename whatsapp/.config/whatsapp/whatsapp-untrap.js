const statusElement = document.querySelector('[aria-label="Status"]');
if (statusElement) statusElement.remove();

const channelsElement = document.querySelector('[aria-label="Channels"]');
if (channelsElement) channelsElement.remove();

const communitiesElement = document.querySelector('[aria-label="Communities"]');
if (communitiesElement) communitiesElement.remove();
